1.ai 排查5大合约的Bug、逻辑完整性
    ComicNFT.sol：
        mint：之前任何人都可以随意铸造二创作品，函数中增加了强制校验逻辑。如果作品声明为二创（ isDerivative = true ），则 msg.sender 必须在原作的 Approved （已批准）授权名单中且授权未过期，否则将触发 Unauthorized 错误。

    Marketplace.sol：
        出价（Offer）和拍卖（Auction）的 ETH 资金已提前锁定在合约中,用户在进行出价或拍卖时,需要先向合约发送 ETH 资金。引入了 isPrepaid 标志位。在 acceptOffer 和 settleAuction 场景下，合约现在会正确使用已锁定的合约余额来支付版税和卖家款项，而不是要求调用者再次支付。

    RoyaltySplitter.sol：

    ComicSocialMarket.sol： 
        ccipReceive：采用的是 “锁定与铸造”（Lock-and-Mint） 模式。在接收到跨链消息后，会自动调用目标链的 ComicNFT.mint 铸造出保留原始元数据和版税信息的封装 NFT，实现了真正的资产跨链转移。这个合约需要与 ComicNFT 合约在“多个”链上部署,并配置好 CCIP 路由。
        Lock-and-Mint 模式是完全可逆的。用户未来可以将目标链上的封装 NFT 销毁，然后在源链上通过一个 bridgeIn （或类似）的函数解锁并取回原始的 NFT。


    BountyBoard.sol： 
        pplyBounty ：要求缴纳 1/10 的保证金。
        新增withdrawDeposit：完善保证金退还逻辑，当申请人未被选中时，应该能够退还保证金。
2.检查项目权限控制：

3.检查常见漏洞，是否有被攻击风险
    ComicSocialMarket跨链NFT
        （1）如何对“同一件事”达成共识？—— 依赖 CCIP 的预言机网络
        （2）如何防止双花（Double Spending）？—— “锁定”机制
        （3）如何防止重放攻击（Replay Attack）？—— 唯一消息 ID，- CCIP 的内置保护 ：CCIP 为每一条跨链消息生成一个唯一的 messageId 。状态校验 ：在 ccipReceive 函数中，有严格的校验：

4.gas优化

5.单元测试，验证核心逻辑（存取款、策略切换、收益计算）的正确性

6.集成测试，验证合约与外部合约（如策略合约）的交互是否正常

7.部署合约到测试网络




一级市场购买：
粉丝支付 1 ETH → [智能合约] 
                  ├─ 0.975 ETH → 创作者
                  └─ 0.025 ETH → 平台财库

二级市场转售：
买家支付 2 ETH → [智能合约]
                  ├─ 0.20 ETH → 原作版税（创作者）
                  ├─ 1.75 ETH → 卖家
                  └─ 0.05 ETH → 平台财库

衍生品销售一级市场购买：
买家支付 1 ETH → [智能合约]
                  ├─ 0.05 ETH → 原创作者版税
                  ├─ 0.925 ETH → 衍生创作者
                  └─ 0.025 ETH → 平台财库

衍生品销售二级市场转售：
买家支付 2 ETH → [智能合约]
                  ├─ 0.2 ETH → 原作版税
                  ├─ 0.1 ETH → 衍生创作者版税
                  ├─ 1.65 ETH → 衍生创作者
                  └─ 0.05 ETH → 平台财库

悬赏任务：
创作者锁定 100 USDC → [智能合约]
                        ├─ 10USDC → 作为保证金申请任务
                        ├─ 100 USDC → 中标者（验收后）
                        └─ 10 USDC → 退回创作者（未中标）
